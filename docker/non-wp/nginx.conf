server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html =404;
    }

    # Emula un sitio no-WP: /wp-json/ no existe → 404 (RF-10).
    location /wp-json/ {
        return 404;
    }
}
